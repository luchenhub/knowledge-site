import{_ as t,o as d,c as s,ap as a,ar as o}from"./chunks/framework.BWFd5M2R.js";const k=JSON.parse('{"title":"<script> 的 defer 和 async 有什么区别？","description":"","frontmatter":{"password":"f364e34a3743eacf1767066da4b2699469e2f9fab3b63fdd112c672a3805497e"},"headers":[],"relativePath":"HTML/脚本的defer与async.md","filePath":"HTML/脚本的defer与async.md","lastUpdated":1773652575000}'),i={name:"HTML/脚本的defer与async.md"};function n(r,e,c,l,p,h){return d(),s("div",null,[...e[0]||(e[0]=[a('<h1 id="script-的-defer-和-async-有什么区别" tabindex="-1"><code>&lt;script&gt;</code> 的 defer 和 async 有什么区别？ <a class="header-anchor" href="#script-的-defer-和-async-有什么区别" aria-label="Permalink to “&lt;script&gt; 的 defer 和 async 有什么区别？”">​</a></h1><p>这道题的关键不是背定义，而是能画出执行时序——面试官最想看的就是你对浏览器解析流程的理解。</p><h2 id="先理解没有任何属性时发生了什么" tabindex="-1">先理解没有任何属性时发生了什么 <a class="header-anchor" href="#先理解没有任何属性时发生了什么" aria-label="Permalink to “先理解没有任何属性时发生了什么”">​</a></h2><img src="'+o+`"><p>从时序图可以直接读出三者的本质差异：</p><p><strong>普通 <code>&lt;script&gt;</code></strong>：遇到标签就停下来，等下载完、执行完，才继续解析 HTML。下载和执行都会阻塞 HTML 解析。</p><p><strong><code>async</code></strong>：下载和 HTML 解析并行进行，但下载完成后立即暂停 HTML 解析来执行脚本。执行时机不确定，取决于网络速度。</p><p><strong><code>defer</code></strong>：下载和 HTML 解析并行进行，但执行被推迟到 HTML 完全解析完、DOM 构建好之后。<code>DOMContentLoaded</code> 事件在所有 <code>defer</code> 脚本执行完后才触发。</p><h2 id="多个脚本时-顺序保证是关键差异" tabindex="-1">多个脚本时，顺序保证是关键差异 <a class="header-anchor" href="#多个脚本时-顺序保证是关键差异" aria-label="Permalink to “多个脚本时，顺序保证是关键差异”">​</a></h2><p><code>async</code> 的执行顺序完全由网络决定，小文件先下完就先执行，HTML 中的书写顺序无效。<code>defer</code> 无论哪个文件先下载完，都会等所有 <code>defer</code> 脚本下载完后按书写顺序依次执行。</p><h2 id="三者核心对比" tabindex="-1">三者核心对比 <a class="header-anchor" href="#三者核心对比" aria-label="Permalink to “三者核心对比”">​</a></h2><table tabindex="0"><thead><tr><th></th><th>普通 <code>&lt;script&gt;</code></th><th><code>async</code></th><th><code>defer</code></th></tr></thead><tbody><tr><td>下载时是否阻塞解析</td><td>是</td><td>否</td><td>否</td></tr><tr><td>执行时是否阻塞解析</td><td>是</td><td>是</td><td>否</td></tr><tr><td>执行时机</td><td>立即（下载后）</td><td>下载完立即</td><td>DOM 解析完成后</td></tr><tr><td>多脚本执行顺序</td><td>有序</td><td>不保证</td><td>有序</td></tr><tr><td>能访问完整 DOM</td><td>否（取决于位置）</td><td>不保证</td><td>是</td></tr><tr><td>适合放在 <code>&lt;head&gt;</code></td><td>会阻塞，不建议</td><td>可以</td><td>推荐</td></tr></tbody></table><h2 id="如何选择" tabindex="-1">如何选择 <a class="header-anchor" href="#如何选择" aria-label="Permalink to “如何选择”">​</a></h2><p><strong>用 <code>defer</code> 的场景</strong>：绝大多数情况。需要操作 DOM 的脚本、有依赖关系的多个脚本（如先加载工具库再加载业务代码）、需要保证执行顺序时。现代框架构建产物多为 <strong><code>type=&quot;module&quot;</code></strong>（见上文「进阶」），模块脚本<strong>默认延迟执行</strong>（行为类似带 <code>defer</code> 的经典脚本），在 HTML 解析完成后按文档顺序执行。</p><p><strong>用 <code>async</code> 的场景</strong>：脚本完全独立，不依赖其他脚本，也不被其他脚本依赖，且不需要操作 DOM。最典型的例子是第三方统计脚本（Google Analytics、埋点 SDK），它们只需要尽早执行，不关心顺序，也不关心 DOM 是否准备好。</p><p><strong>普通 <code>&lt;script&gt;</code></strong>：如果必须放在 <code>&lt;head&gt;</code> 里，强烈建议加 <code>defer</code> 或 <code>async</code>；若放在 <code>&lt;/body&gt;</code> 之前（传统做法），效果接近 <code>defer</code>，但现代项目已不推荐这种方式。</p><h2 id="进阶-模块脚本-type-module" tabindex="-1">进阶：模块脚本 <code>type=&quot;module&quot;</code> <a class="header-anchor" href="#进阶-模块脚本-type-module" aria-label="Permalink to “进阶：模块脚本 type=&quot;module&quot;”">​</a></h2><p>打包工具（Vite、webpack 等）产出的入口常为 <strong><code>&lt;script type=&quot;module&quot;&gt;</code></strong>，与经典脚本不同：</p><ul><li><strong>默认延迟执行</strong>：模块在 <strong>HTML 解析完成之后</strong>再跑，整体语义上<strong>接近</strong>「带 <code>defer</code> 的经典脚本」——不阻塞解析、按文档顺序执行多个模块依赖图。</li><li><strong>严格模式</strong>、顶层的 <code>import</code> / <code>export</code>、独立模块作用域；可加 <code>async</code> 表示<strong>并行取依赖、尽快执行</strong>（与经典 <code>async</code> 类似，但作用于模块图）。</li><li>面试可答：「现代项目多 ES Module，<strong>默认不会在解析中途执行</strong>，和手写 <code>defer</code> 的经典脚本在「时机」上类似；细究规范时与 <code>defer</code> 仍不是同一套机制，不必背成一字不差。」</li></ul><h2 id="一个容易答错的追问" tabindex="-1">一个容易答错的追问 <a class="header-anchor" href="#一个容易答错的追问" aria-label="Permalink to “一个容易答错的追问”">​</a></h2><p><strong>&quot;<code>defer</code> 脚本一定在 <code>DOMContentLoaded</code> 之前执行吗？&quot;</strong></p><p>是的。规范顺序是：先按文档顺序执行完<strong>所有</strong> <code>defer</code> 脚本，再触发 <code>DOMContentLoaded</code>。因此单个 <code>defer</code> 脚本跑完时，事件还没派发。</p><p>那在 <code>defer</code> 脚本里监听 <code>DOMContentLoaded</code> 有用吗？<strong>有用</strong>：此时事件尚未触发，你在脚本里注册的监听器会在「全部 <code>defer</code> 脚本都执行完」之后被调用。若你只是要等「当前脚本执行时 DOM 已就绪」，一般<strong>不必</strong>再包一层 <code>DOMContentLoaded</code>，直接写逻辑即可；若你要在「页面上所有 <code>defer</code> 脚本都跑完」之后再做事（例如依赖后面某个 <code>defer</code> 脚本暴露的全局），用 <code>DOMContentLoaded</code> 仍然合理。</p><div class="language-js"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 仅「DOM 已就绪」时，下面这层监听通常多余——直接写业务即可</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">document.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">addEventListener</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;DOMContentLoaded&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 会执行：发生在所有 defer 脚本之后</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// defer 保证执行到这里时文档已解析完，可直接操作 DOM</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">document.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">querySelector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;#app&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).textContent </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;已挂载&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div>`,24)])])}const f=t(i,[["render",n]]);export{k as __pageData,f as default};
