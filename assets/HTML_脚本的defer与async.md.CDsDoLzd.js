import{_ as t,o as d,c as a,ap as s,ar as i}from"./chunks/framework.BWFd5M2R.js";const g=JSON.parse('{"title":"","description":"","frontmatter":{"password":"f364e34a3743eacf1767066da4b2699469e2f9fab3b63fdd112c672a3805497e"},"headers":[],"relativePath":"HTML/脚本的defer与async.md","filePath":"HTML/脚本的defer与async.md","lastUpdated":1773652575000}'),n={name:"HTML/脚本的defer与async.md"};function r(o,e,c,h,l,p){return d(),a("div",null,[...e[0]||(e[0]=[s('<h2 id="script-的-defer-和-async-有什么区别" tabindex="-1"><code>&lt;script&gt;</code> 的 defer 和 async 有什么区别？ <a class="header-anchor" href="#script-的-defer-和-async-有什么区别" aria-label="Permalink to “&lt;script&gt; 的 defer 和 async 有什么区别？”">​</a></h2><p>这道题的关键不是背定义，而是能画出执行时序——面试官最想看的就是你对浏览器解析流程的理解。</p><h2 id="先理解没有任何属性时发生了什么" tabindex="-1">先理解没有任何属性时发生了什么 <a class="header-anchor" href="#先理解没有任何属性时发生了什么" aria-label="Permalink to “先理解没有任何属性时发生了什么”">​</a></h2><img src="'+i+`"><p>从时序图可以直接读出三者的本质差异：</p><p><strong>普通 <code>&lt;script&gt;</code></strong>：遇到标签就停下来，等下载完、执行完，才继续解析 HTML。下载和执行都会阻塞 HTML 解析。</p><p><strong><code>async</code></strong>：下载和 HTML 解析并行进行，但下载完成后立即暂停 HTML 解析来执行脚本。执行时机不确定，取决于网络速度。</p><p><strong><code>defer</code></strong>：下载和 HTML 解析并行进行，但执行被推迟到 HTML 完全解析完、DOM 构建好之后。<code>DOMContentLoaded</code> 事件在所有 <code>defer</code> 脚本执行完后才触发。</p><h2 id="多个脚本时-顺序保证是关键差异" tabindex="-1">多个脚本时，顺序保证是关键差异 <a class="header-anchor" href="#多个脚本时-顺序保证是关键差异" aria-label="Permalink to “多个脚本时，顺序保证是关键差异”">​</a></h2><p><code>async</code> 的执行顺序完全由网络决定，小文件先下完就先执行，HTML 中的书写顺序无效。<code>defer</code> 无论哪个文件先下载完，都会等所有 <code>defer</code> 脚本下载完后按书写顺序依次执行。</p><h2 id="三者核心对比" tabindex="-1">三者核心对比 <a class="header-anchor" href="#三者核心对比" aria-label="Permalink to “三者核心对比”">​</a></h2><table tabindex="0"><thead><tr><th></th><th>普通 <code>&lt;script&gt;</code></th><th><code>async</code></th><th><code>defer</code></th></tr></thead><tbody><tr><td>下载时是否阻塞解析</td><td>是</td><td>否</td><td>否</td></tr><tr><td>执行时是否阻塞解析</td><td>是</td><td>是</td><td>否</td></tr><tr><td>执行时机</td><td>立即（下载后）</td><td>下载完立即</td><td>DOM 解析完成后</td></tr><tr><td>多脚本执行顺序</td><td>有序</td><td>不保证</td><td>有序</td></tr><tr><td>能访问完整 DOM</td><td>否（取决于位置）</td><td>不保证</td><td>是</td></tr><tr><td>适合放在 <code>&lt;head&gt;</code></td><td>会阻塞，不建议</td><td>可以</td><td>推荐</td></tr></tbody></table><h2 id="如何选择" tabindex="-1">如何选择 <a class="header-anchor" href="#如何选择" aria-label="Permalink to “如何选择”">​</a></h2><p><strong>用 <code>defer</code> 的场景</strong>：绝大多数情况。需要操作 DOM 的脚本、有依赖关系的多个脚本（如先加载工具库再加载业务代码）、需要保证执行顺序时。现代框架（React、Vue）的构建产物默认就是 <code>defer</code>。</p><p><strong>用 <code>async</code> 的场景</strong>：脚本完全独立，不依赖其他脚本，也不被其他脚本依赖，且不需要操作 DOM。最典型的例子是第三方统计脚本（Google Analytics、埋点 SDK），它们只需要尽早执行，不关心顺序，也不关心 DOM 是否准备好。</p><p><strong>普通 <code>&lt;script&gt;</code></strong>：如果必须放在 <code>&lt;head&gt;</code> 里，强烈建议加 <code>defer</code> 或 <code>async</code>；若放在 <code>&lt;/body&gt;</code> 之前（传统做法），效果接近 <code>defer</code>，但现代项目已不推荐这种方式。</p><h2 id="一个容易答错的追问" tabindex="-1">一个容易答错的追问 <a class="header-anchor" href="#一个容易答错的追问" aria-label="Permalink to “一个容易答错的追问”">​</a></h2><p><strong>&quot;<code>defer</code> 脚本一定在 <code>DOMContentLoaded</code> 之前执行吗？&quot;</strong></p><p>是的，规范要求所有 <code>defer</code> 脚本执行完成后，才会触发 <code>DOMContentLoaded</code>。所以在 <code>defer</code> 脚本内监听 <code>DOMContentLoaded</code> 是无效的——事件永远不会触发，因为你的脚本本身就是在它之前跑完的。正确的做法是直接在 <code>defer</code> 脚本里写逻辑，不需要包一层 <code>DOMContentLoaded</code> 监听器。</p><div class="language-js"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// defer 脚本里这样写是多余的——DOMContentLoaded 此时已经触发过了</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">document.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">addEventListener</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;DOMContentLoaded&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 这个回调永远不会执行</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 直接写就好，defer 保证 DOM 已经就绪</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">document.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">querySelector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;#app&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).textContent </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;已挂载&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div>`,20)])])}const f=t(n,[["render",r]]);export{g as __pageData,f as default};
